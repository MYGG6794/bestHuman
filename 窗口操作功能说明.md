# 窗口拖动和调整大小功能使用说明

## 新增功能

### 🖱️ 窗口拖动和调整大小
现在您可以在**禁用点击穿透**时自由拖动和调整视频流窗口：

#### 拖动窗口
- 在窗口内容区域按住鼠标左键并拖动即可移动窗口位置
- 释放鼠标后窗口位置会自动保存

#### 调整窗口大小
- 将鼠标移动到窗口边缘（8像素范围内）
- 鼠标光标会变为调整大小的双箭头
- 按住鼠标左键拖动即可调整窗口大小
- 支持8个方向的调整：上、下、左、右、左上、右上、左下、右下
- 最小窗口大小：200x150像素
- 释放鼠标后窗口大小会自动保存

### ⌨️ 快捷键操作

#### Alt + T - 切换点击穿透状态 🆕
- **禁用点击穿透**：可以拖动和调整窗口大小
- **启用点击穿透**：鼠标点击会穿透到桌面下方的应用

#### 其他快捷键
- **Alt + S** - 打开/关闭设置窗口
- **Alt + C** - 打开/关闭抠像控制窗口
- **Alt + A** - 打开/关闭AI管理窗口

### 🔧 系统托盘功能
右键点击托盘图标可以快速访问：
- 显示主窗口
- 设置
- 抠像控制
- **切换点击穿透 (Alt+T)** 🆕
- AI管理
- 退出

### 📊 状态栏显示
窗口底部状态栏实时显示：
- 主要状态信息
- 抠像启用状态
- WebSocket连接状态
- 点击穿透状态提示

## 推荐使用流程

1. **首次启动**：程序默认禁用点击穿透，您可以自由拖动窗口
2. **调整位置和大小**：拖动窗口到合适位置，调整到合适大小
3. **设置抠像参数**：使用 Alt+C 打开抠像控制，调整抠像效果
4. **启用点击穿透**：调整完成后，使用 Alt+T 启用点击穿透
5. **日常使用**：需要调整时，再次按 Alt+T 禁用点击穿透

## 注意事项

- 启用点击穿透后，鼠标无法直接操作窗口，需要用热键或托盘菜单
- 所有窗口位置和大小变更都会自动保存到配置文件
- 状态栏会显示当前的点击穿透状态
- 托盘气泡提示会提醒您当前状态

现在您可以更方便地调整视频流窗口了！🎉
