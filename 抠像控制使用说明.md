# 抠像控制功能使用说明

## 功能概述
我已经为您集成了一个实时抠像参数控制界面，让您可以精确调节抠像效果，而不需要依靠猜测。

## 打开抠像控制窗口的方式

### 方式1：全局热键（推荐）
- 按 `Alt + C` 组合键即可打开/关闭抠像控制窗口
- 这个热键在任何时候都可以使用，即使主窗口没有焦点

### 方式2：设置界面按钮
1. 按 `Alt + S` 打开设置界面
2. 在"抠像设置"组中，点击"实时调节"按钮
3. 抠像控制窗口会立即弹出

## 抠像控制界面功能

### 基本控制
- **启用抠像**：复选框，控制是否启用抠像功能
- **颜色选择**：点击颜色块可以选择要抠除的颜色（默认绿色）

### 精细调节参数
1. **绿色阈值**：控制绿色分量的最小值
   - 值越高，要求绿色越强才会被抠除
   - 可以避免浅绿色被误抠

2. **颜色容差**：控制颜色匹配的容忍度
   - 值越大，抠除范围越宽泛
   - 值越小，抠除更精确

3. **最小亮度**：设置抠除区域的最小亮度
   - 避免过暗的区域被抠除
   - 保护阴影细节

4. **最大亮度**：设置抠除区域的最大亮度
   - 避免过亮的反光区域被抠除
   - 保护高光细节

### 预设选项
- **精确模式**：严格匹配，减少误抠
- **宽松模式**：宽泛匹配，更容易抠除
- **平衡模式**：兼顾精确性和完整性

### 实时预览
- 所有参数调节都会立即应用到视频流
- 可以实时看到抠像效果的变化
- 支持撤销和重置功能

## 使用建议

### 调节流程
1. 首先确保数字人视频流正常加载
2. 按 `Alt + C` 打开抠像控制窗口
3. 开启"启用抠像"复选框
4. 根据实际背景调节参数：
   - 如果背景没有完全抠透：增加容差或降低绿色阈值
   - 如果数字人被误抠：降低容差或提高绿色阈值
   - 如果边缘有杂色：调节亮度范围

### 常见场景
- **标准绿幕**：使用默认设置或精确模式
- **非均匀绿幕**：适当增加容差
- **光照不均**：调节亮度范围
- **边缘毛刺**：降低容差，调节绿色阈值

## 保存和恢复
- 所有参数调节会自动保存到配置文件
- 下次启动程序时会自动加载上次的设置
- 设置界面中的基础参数会与抠像控制同步

## 技术说明
- 抠像算法基于实时JavaScript像素处理
- 支持WebRTC视频流的实时抠像
- 参数变更立即生效，无需重启
- 窗口支持置顶显示，不影响其他操作

现在您可以精确控制抠像效果，获得最佳的数字人显示质量！
